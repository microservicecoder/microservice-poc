{
  //if we want to override the configurations from appsetting.json in dev environment then
  // we will use the appsetting.Development.json file by having the development configurations
  // here. Like wise we can use the respective environment configurations such as staging/ production
  //etc.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
